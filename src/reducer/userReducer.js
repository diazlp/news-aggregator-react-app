const initialState = {
  userLoading: false,
  isUserLoggedIn: false,
}

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default userReducer;
